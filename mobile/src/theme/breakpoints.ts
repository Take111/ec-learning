// Web レスポンシブの語彙(ネイティブの画面コードはここを参照しない)。
// 前提: 分岐は「モバイル幅 → md → lg」の3段だけに抑える。段を増やしたくなったら
//   それはレイアウト設計の複雑化のサインなので、まず画面側の構造を疑う
export const breakpoints = {
  md: 768, // 商品グリッド3列・詳細2カラムが成立し始める幅
  lg: 1024, // 商品グリッド4列
} as const;

// 中央寄せコンテンツの上限幅。
// narrow: フォーム・明細系(カート/チェックアウト/履歴)— 1カラムの読みやすさ優先
// wide: グリッド・2カラム詳細 — ヘッダーナビ(site-header)の内幅もこれに揃える
export const contentWidth = {
  narrow: 640,
  wide: 1024,
} as const;
